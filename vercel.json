{
  "buildCommand": "yarn build:vigil:cloud",
  "installCommand": "yarn install",
  "outputDirectory": "platform/app/dist",
  "rewrites": [
    {
      "source": "/pacs/:path*",
      "destination": "/api/pacs/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
